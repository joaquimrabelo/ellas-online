import styled from 'styled-components';

export const Container = styled.section`
  margin-top: 30px;

  .container {
    flex-direction: row;
    justify-content: flex-start;
    flex-wrap: wrap;
  }
`;